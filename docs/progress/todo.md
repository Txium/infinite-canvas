---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 在后台为 302.AI、WaveSpeed、LEC、seedance.nz 填入已核实的 Base URL，并在 Render Secret 配置四个 `MODEL_PROVIDER_*_API_KEY` 环境变量，逐家小额充值并测试。
- 根据三家官方文档核对实际上游模型 ID、请求协议和当前成本，不把宣传页价格直接写入生产价格表。
- 为 WaveSpeed 和 seedance.nz 实现各自的创建任务、状态查询和结果归一化适配器；LEC 在官方接口身份和文档确认后再接入。
- 完成不同规格的价格选择与计费：分辨率、时长、模式和参考素材数量共同决定冻结人民币余额。
- 大语言模型按上游返回的实际输入/输出 Token 分别结算，Kimi 和 GLM 在取得实时成本后自动应用成本加 10%，不得按一次请求固定扣费。
- 在取得各家正式余额查询文档后，将中转站管理中的手工余额升级为自动查询；查询失败时保留最近一次核实值和更新时间，不伪造实时余额。
- 在上游返回可核验的实际成本后，为管理后台补充按供应商汇总的成本、退款和毛差；当前任务筛选与真实收入已完成，上游未返回真实成本前不得伪造利润。
- 取得易支付兼容商户的网关地址、商户 ID 与商户密钥后，在 Render 配置 `EPAY_API_URL`、`EPAY_MERCHANT_ID`、`EPAY_MERCHANT_KEY`，并用小额订单验证微信、支付宝异步回调和自动到账。
- 配置一个平台 S3/R2 或 WebDAV 对象存储并验证生成结果转存；配置前生成结果仍可能依赖上游临时链接。
