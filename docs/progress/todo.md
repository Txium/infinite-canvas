---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

## 本轮排查后的上线阻塞项

- [x] 视频重试复用旧失败编号的问题已修复；新增独立且幂等的重试任务和资金状态保护。
- [ ] 视频重试的真实成片、播放和账务验收仍需一次明确预算的任务。

- [x] LEC 900 视频 multipart 转 JSON/images 及扣费前参数校验已实现，模拟测试通过。
- [ ] 部署后以明确单次预算验证 LEC 900 图生视频成片、播放及结算；其他视频渠道仍需各自真实验收。

- [x] 2026-09-12：修复节点模型与全局模型显示错配，统一价格与提交配置；补齐 302 Midjourney 双重鉴权及安全错误提示。本地 12 项前端回归、全仓 Go 测试、go vet 和生产构建通过。
- [x] 上述修复 `b4b59d6` 已部署 staging，模型切换、报价与整页刷新检查通过；原画布最终保留 GPT Image 2 Low 1K / ¥0.09。
- [ ] 在明确单次预算后实测图片与视频；免费界面检查和模拟测试不代表真实供应商生成成功。

- 2026-09-10：最终代码 `7a13a0c` 已部署 staging，健康检查通过，原画布保留。尺寸监听、手机标题、390/622/1280px、双侧栏、助手及主题已实测；后台余额刷新和测试素材保存/刷新/搜索通过。
完整对应关系和验证证据见 [前后端排查报告](audit-report.md)。修复代码位于 `staging-wallet`，未产生付费生成。已登录 staging 管理员并确认 PostgreSQL/存储就绪标记；只新建一张验收画布、上传项目公开 Logo，原有两张画布未修改。用户已自行登录 Render，本次只更新 staging，不更新正式站 `main`。

- [ ] 在 staging PostgreSQL 验证图片/音频新增 `upstream_task_id` 列及金融事务并发；SQLite 测试不替代 PostgreSQL 行锁和历史数据迁移验收。
- [ ] 使用真实供应商、明确的小额预算逐条验证图片/视频/语音/数字人/3D；核对实际画幅、时长、原始参考素材、结果可播放及上游扣费。
- [ ] 动态 Token / 原币成本结算仍未实现；本轮已禁止这类档位生成，不能靠填写价格公式或最低预冻结金额提前开放。
- [ ] 非 WaveSpeed 图片/音频以及没有获得上游任务 ID 的不确定请求，仍需供应商正式查询协议或人工对账入口；保持冻结，不自动重新下单或退款。
- [ ] 核验平台对象存储真实上传、读回、跨设备访问及失败重试；无凭据时不能把上游临时 URL 当作永久存储。
- [ ] 支付宝审批、真实回调、退款和支付手续费对账需在 staging 实付验证；本轮未打开收款开关。
- [ ] 补齐实际成本回传、账单调整；包含估算的成本和利润已明确标注，不能当作核实利润。
- [x] 原币余额持久化代码、回归和 staging 刷新验收完成：保留原值/币种/成功时间，查询失败不清空，旧请求与配置编辑不得覆盖快照。
- [ ] 剩余浏览器/云端交互验收：跨账号切换、真实退款防连点、冷启动故障、付费任务恢复/播放、管理员筛选和 3D 下载。窄屏/主题/助手、视图刷新、图片读回、文本素材搜索、钱包与财务读取已通过；未做付费生成。

## 原有待办（仍需外部验收）

- 四家 Provider 的 Base URL 与 Render Secret 已接入；仍需为 302.AI Key 开通余额查询权限，并分别用小额任务验证已启用线路、结算和失败解冻。
- 根据三家官方文档核对实际上游模型 ID、请求协议和当前成本，不把宣传页价格直接写入生产价格表。
- 继续依据 WaveSpeed、LEC 和 seedance.nz 的正式文档补充各家特殊状态与结果字段；通用异步创建、轮询及画布结果恢复已经接通，仍需逐个模型实测确认。
- 完成不同规格的价格选择与计费：分辨率、时长、模式和参考素材数量共同决定冻结人民币余额。
- 大语言模型按上游返回的实际输入/输出 Token 分别结算，Kimi 和 GLM 在取得实时成本后自动应用成本加 10%，不得按一次请求固定扣费。
- 在取得各家正式余额查询文档后，将中转站管理中的手工余额升级为自动查询；查询失败时保留最近一次核实值和更新时间，不伪造实时余额。
- 在上游返回可核验的实际成本后，为管理后台补充按供应商汇总的成本、退款和毛差；当前任务筛选与真实收入已完成，上游未返回真实成本前不得伪造利润。
- 支付宝 RSA2、App ID、商户号和 Render Secret 已配置；接入持久化 PostgreSQL 后，用其他支付宝账号做小额订单验证异步回调、自动到账和原路退款。
- 配置一个平台 S3/R2 或 WebDAV 对象存储并验证生成结果转存；配置前生成结果仍可能依赖上游临时链接。
- 在 staging 使用生产数据库结构验证兼容迁移，并用支付宝测试订单验证 `seller_id`、`trade_no` 唯一约束和重复 notify；验证完成前不部署生产。
- 图片、音频和动态 Token 模型后续接入可核验的实际上游成本回传；当前视频任务可先按模型档位成本记为 `estimated`，账单确认后必须用追加调整流水校正。
- 302.AI、WaveSpeed、seedance.nz 原始余额独立存储已部署并完成刷新验收；302 实际 Key 的余额权限仍返回 403，不能用代码伪造余额。
- 支付宝产品提额材料仍等待审核；审核通过且持久化数据库接入后再开放小额实付验证。
- staging 对象存储已配置，Logo 上传与部署后读回通过；生成结果转存、跨设备访问和持久化失败重试仍待专项验收。
- AI 超分已接通当前图片模型，但仍需小额实付验证高保真提示词在各条图片线路上的稳定性；未验证前不承诺完全逐像素一致。
# 2026-08-30 生成任务历史回收与媒体预览

- [x] 将缺失于 `video_tasks` 的 WaveSpeed/海螺、LEC/Seedance 历史 AI 调用日志合并到管理员生成任务列表。
- [x] 展示中转站名称与上游任务 ID，保留成功、失败和原始错误原因。
- [x] 管理员生成任务页直接预览图片、视频和音频结果，不再只有“查看”文字链接。
- [x] 部署后核对昨天海螺与 Seedance 2.0 历史记录及预览；后台现有 8 条记录、4 个可播放视频，其中包含 2 条 WaveSpeed MiniMax H3 历史结果。
# 2026-08-30 视频任务全生命周期对账

- [x] 长时间运行的视频不再被本地超时直接判为失败和解冻，改为“等待上游确认”。
- [x] 后台继续按上游任务 ID 对账，只有明确成功才结算、明确失败才解冻。
- [x] 管理员生成任务增加固定“生成效果”列和放大预览弹窗。
- [x] 部署后验证管理员后台历史视频、图片预览和失败日志；放大播放器已成功加载 WaveSpeed H3 视频。
