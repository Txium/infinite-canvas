---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

## 免费Media Worker接通

- 最终Web版本`56cb767` / Render `dep-dajpm8lg1s2s73bn218g` Live，日志确认`MEDIA_SCHEMA_MIGRATION_APPLIED version=001_media_profiles driver=postgres`。
- 实际staging数据库是Supabase PostgreSQL（连接池），Production为另一Neon主机；本次只应用staging迁移，正式环境只读核对。
- 测试画布新增「部署验收角色（未配置音色）」档案，POST保存成功；整页刷新后同一char_角色ID和名称读回，节点关联保留。未创建试听或付费任务。

- 独立Worker `srv-dajpevek1f9s738u5920` / `dep-dajpevmk1f9s738u5a9g` Live；健康检查FFmpeg/ffprobe均可用。staging URL/TOKEN已配置，令牌未写仓库或公开日志。
- 原画布工作台经API读取现有15秒视频元数据成功，余额仍¥8.22；用户自行验收输出。无轨视频提音频明确VIDEO_HAS_NO_AUDIO，mute是另一个无声视频操作，不是音频结果。
- 免费实例$0/月，不买GPU，前端24MB/Worker25MB单任务，冷启动可能较慢。
- 角色稳定ID、跨节点关联、reference_images与版本化PostgreSQL迁移已补，发布后检查迁移日志及表单读回。

## 本批staging发布回执

- `bd6eddc` / `dep-dajp02fqj5pc73eh17v0` 云端构建、启动并Live；新工具入口及十种禁用AI能力现场可见，Voice Profile列表读取无错误。档案POST及重新读取仍待验收。
- 原画布图片/视频与余额¥8.22保留；没有点击生成。视频处理弹窗真实提示Worker未部署，不能把这次发布当作媒体运算已经启用。
- 未购买新服务、未配置Provider、未改Production。独立Media Worker仍需部署资源决定。

## 媒体基础能力与音频架构补齐（未发布）

- 低清8格时间轴、当前帧接生图/下一镜、截帧来源标准字段、AAC/MP3复制、无声视频、Worker临时目录TTL。
- 十种媒体AI能力禁用入口、能力API/任务提交预留、Adapter和任务结构；已知未启用请求明确503且不扣费。
- 角色音色数据库/当前用户GET和POST接口/节点内表单；不代表TTS/音色设计/克隆模型已接通。
- 完整P0–P3实现与缺口见 [媒体交付边界](media-capabilities.md)。Worker云端部署、PostgreSQL迁移、真实FFmpeg输出和页面验收仍未完成。

## LibTV截图对照补充（代码完成，未发布）

- 图片创作增加人像质感调节、打光，使用连接的原图创建新生成节点，沿用来源模型/渠道/画幅，不自动提交付费请求。属于参考图提示词工作流，不是专用无损滤镜。
- 图片创作面板集中提供现有多角度、元素编辑、宫格切分、放大、AI超分入口；九宫格生成和现图切分明确区分。
- Media Worker增加视频流复制输出无声视频，与提取音频配合使用；原视频保留。Worker未部署，实际输出未验证。
- 本批不涉及钱包、Provider、Production；图片效果由用户自测，不宣称真实模型效果通过。

## 视频处理与图片创作首批实现（待用户验收）

- `1e07b85` 已发布staging，Render `dep-dajntbu7bikc73cqi5ag` 确认Live；现场菜单显示四个图片创作选项，测试画布新增九宫格节点与预设提示词，余额仍¥8.22，没有点击生成。
- 界面检查发现顶部视频节点的悬浮工具栏越界，补充下方定位；后续小版本需重新部署验证。

- 图片节点增加图片创作：九宫格分镜、细节特写、人物三视图、球形全景；新建节点并连接参考图，不自动生成或扣费。
- 视频节点增加处理面板：原视频播放器/时间轴/真实帧时间戳步进、首尾当前帧、尾帧接下一镜、片段裁剪、抽音频、移除独立字幕轨。
- 新增独立Python FFmpeg Worker及Dockerfile，通过登录API代理上传原视频；Worker秘钥仅服务器持有、请求大小与并发受限；不访问Provider和钱包。
- Worker尚未在staging部署，当前机器也未安装FFmpeg，真实媒体输出待用户测试；已添加命令/接口回归，不能将入口和单元测试当成媒体验收通过。
- 后续音频/GPU、Voice Profile与音色设计还未补齐，详见审计清单；没有伪造为已完成。

## 全链路审计启动

- 新增完整审计与验收清单，记录真实代码风险、staging Provider只读结果、未验证模型与Worker依赖。本轮暂未发布生成链路改动或付费测试，不能称为已修复。

## 真实视频验收与查询误报修复

- LEC Seedance 2.0 900 图生视频已真实完成：1280×720、15.103991 秒，浏览器完整播放至 ended，无媒体错误；单笔 ¥1.69 已结算。
- 查询限流日志被历史任务恢复逻辑错误展示为独立失败任务，已排除 GET 诊断日志，原始 AI 日志保留；后台查询间隔调整为 10 秒。
- 视频创建与任务状态变化刷新钱包；重试编号纳入本地任务标识保护。`890788c` 已部署 staging，Render `dep-dajnfkoae00c73alsagg` 确认 Live；全仓 Go 测试、15 项前端回归和生产构建通过。
- 部署后后台任务数从 17 条恢复为 5 条真实记录，查询限流不再误报；画布刷新后视频仍为 15.103991 秒、1280×720、readyState 4，无媒体错误，余额 ¥8.22。

## 图生视频重试参考图修复

- 用户授权真实视频测试后，首次重试被扣费前校验拦截：缺少参考图，余额仍 ¥9.91。
- 原因：重试沿图片祖先查找生图配置，跳过视频直接连接的成功图片。已改为存在直接图片输入时保留视频节点上下文，新增配置→图片→视频回归。
- 14 项前端回归通过；部署后真实单次视频已完成并完整播放，未重复下单。

## GPT Image 2 档位可选性修复

- staging 后台原有 9 个上架档位，但仅 Low 1K 路由启用。核对 WaveSpeed 当前模型文档及后端分辨率映射后，已通过管理员界面恢复其余 8 条相同上游接口的线路；没有修改价格或其他模型路由。
- 原画布模型菜单已实测显示 Low/Medium/High × 1K/2K/4K 共 9 档，图片选项总数从 7 增至 15。验收画布新增空图片和生成配置节点，选择 Low 2K，价格 ¥0.16/张；本轮没有付费生成。
- 增加全部 9 档的请求参数回归，验证质量、分辨率及横屏比例不会被全局默认覆盖。真实 2K/4K 输出尺寸与扣费仍需明确预算后验收。

## 视频重试任务编号修复

- 定位到视频重试复用 `client_video_task_节点ID`，后台幂等查询直接返回旧失败记录。前端现在携带明确的原任务编号，后台仅在原任务属于当前用户、已失败且资金解冻后生成独立重试编号。
- 同一原任务的并发点击/网络重发映射同一替代任务；处理中、资金仍冻结和成功任务不重新下单。原金融事务和历史任务不改写。
- `c7b9ba5` 已部署 staging，Render `dep-daiine5g1s2s73fgrpng` 确认 Live；全仓 Go 测试、go vet、13 项前端回归、类型检查和生产构建通过。原画布已刷新到新版本，保留图片与视频节点，余额仍 ¥9.91。
- 后续已完成授权的单次 LEC 900 成片验收，详见本页真实视频验收记录；其他渠道不视为已通过。

## LEC 900 图生视频请求修复

- 修复现有参考图已转 HTTPS 链接、但整个视频请求仍按 multipart 转发的问题；LEC `lec-seed-2-0-900` 转为官方示例的 JSON `model/prompt/aspect_ratio/images`，保留多张参考图顺序。
- 请求在冻结余额前校验：15 秒、横屏/竖屏、最多 9 图、不接受原始文件与不支持的音视频/尾帧，错误直接提示，避免本地参数错误冻结余额。
- 新增 multipart 多图、JSON、非法链接/画幅/时长/文件和其他渠道不受影响的回归。全仓 Go 测试通过；真实付费视频未重试，生成成功仍待明确预算后验收。
- 协议依据：https://api.paipu.net/docs/videos/lec-seed-2-0-900 。未更改其他 LEC 模型协议、价格、支付设置或站内余额。
- `d65942c` 已在 staging 部署成功（Render `dep-daiii1ek1f9s738bn54g`），服务日志确认 live，`/api/health` 返回 ok；全仓 Go 测试、go vet 和差异检查通过。原画布仍保留 GPT 成功图片和历史视频失败节点，显示余额 ¥9.91；未提交新的付费任务。

## 2026-09-12 模型选择与 302 鉴权修复

- 已观察的两条图片失败任务实际为 Midjourney/302，错误为 `Missing 302 Apikey`，均已解冻；不能据此判断 GPT Image 2 上游失败。
- 配置框此前显示全局模型，而价格与提交使用节点模型。现在四种生成模式共用同一解析函数，模型选择器显示实际提交模型，保留无效模型回退与节点参数。
- 302 Midjourney 提交和轮询均携带网关 Authorization 与 mj-api-secret；认证错误转换为不含上游调试内容的明确提示。
- 本地验证通过：前端 12 项回归、全仓 Go 测试（6 个有测试包）、go vet、Next.js 生产构建与类型检查。
- staging 已部署 `b4b59d6`，Render `dep-daii7dbm8hqs73cvjkvg` 显示 Deploy succeeded / Live（2026-09-12 18:07）。原画布刷新后正确显示 Midjourney/¥0.39；切换 GPT Image 2 Low 1K 后显示 ¥0.09，整页刷新仍保持一致；视频模式显示 Seedance 2.0 人物特惠 720P / 900、15s、¥1.69，最后恢复 GPT 图片模式。
- 待验收：真实图片/视频结果、扣费与失败解冻需明确预算后测试。本轮未提交付费生成，未更改余额或支付配置，页面余额仍为 ¥10。旧失败节点保留，历史错误不会被新代码改写。

## 本轮已部署修复与剩余验收

详细检查矩阵见 [前后端排查报告](audit-report.md)。2026-09-10 全仓 Go 150 个测试及子测试、前端 6 项回归、TypeScript、Next.js 生产构建和 `go vet` 均通过。最终代码 `7a13a0c` 已在 staging 部署成功；所有生成/金融测试均为本地模拟，没有真实生成收费。

### 2026-09-10 侧面画布实测与后续修复

- [x] `2ed0dbb` 在 staging 上线；622px 下正确显示紧凑标题栏、侧栏浮层和工具栏，侧栏选择节点自动收起；390px 助手可展开/关闭。
- [x] 390px 验收发现标题按钮的 truncate 被全局 overflow:visible 覆盖；已显式限制裁切并补充助手收起按钮标签，最终部署后截图确认标题不再覆盖配置按钮。
- [x] 首批修复 `0f61d40` 已在 Render staging 成功部署，健康检查通过，原有画布仍保留；正式站未修改。
- [x] 新版实测发现异步加载导致尺寸监听未绑定；已改为加载完成后绑定并保留视图，新增生命周期/缩放/卸载回归。最终线上刷新前后视图变换完全一致，图片可读；前端测试 6 项、类型检查与生产构建通过。
- [x] staging 管理员登录成功，财务页面报告 PostgreSQL 持久化与对象存储已配置；这不是新增列已迁移或金融并发已验证的证据。
- [x] 新建“修复验收 2026-09-10”，重命名、上传公开 `logo.svg` 成功；刷新后仍显示 1 个节点，站内 `/api/files/.../content` 图片可读取。保留测试画布，原有两张画布未修改。
- [x] 线上查询观察：WaveSpeed 返回 5.92 USD；seedance.nz 返回 10.38 CUSTOM；302 返回余额权限 403；LEC 可列模型但暂无已核实余额端点。均为当次快照，不代表以后余额或生成质量。
- [x] 本地补齐原币余额持久化及失败保留/乱序保护；后台保存与登记按钮防连点；数据库、接口回归通过。
- [x] 本地修复窄屏标题遮挡、侧栏挤占和底部工具栏固定偏移；类型检查、生产构建通过。
- [x] 自动转存查询真实用户身份；音频以原字节转存，记录 SHA256；已存储结果不被迟到临时 URL 覆盖。模拟存储与数据库回归通过。
- [x] 用户已自行登录 Render；只部署 `staging-wallet`，正式站 `main` 未变；指定提交部署后 Auto-Deploy 已关闭。
- [x] 最终线上 390px/622px/1280px、双侧栏和助手展开收起、浅深主题已检查；测试 Logo 部署后读回成功，原有两张画布未修改。
- [x] 后台余额刷新后保留原值及核实时间；我的素材文本保存、整页刷新、匹配搜索、无结果和清空搜索通过。
- [x] 钱包读取旧订单与财务估算文案正常；注意登录完成后“立即支付”已可点，本轮未改支付配置或付款，不能称作收款已关闭或实付已验收。
- [ ] 真实生成音频转存、跨设备/跨账号、金融并发与小额实付仍需专项验收；不把本地模拟或一次上传当作全链路成功。
- [ ] 对象转存失败的持久化重试队列与云端跨设备/跨账号专项验收仍待完成，不因一次 Logo 上传成功就判定全部存储功能完成。

- 修复钱包旧请求覆盖新账号/新余额，以及旧 401 响应误登出新账号；钱包与财务读取忽略过期响应，退款申请和运营费用登记防连续提交。
- 修复视频结算丢失刚取得的结果，以及退款后迟到成功误扣其他任务冻结金额；重复结算/解冻互斥，跨用户账务 ID 重放被拒绝。
- 修复外部伪造内部计费请求头、multipart 真实模型 ID 未替换、非法生成数量/时长和金额溢出；保留自动时长 `-1` 的 15 秒冻结约定。
- 上游已接单的 WaveSpeed 图片/音频保存任务号；查询 503 等不确定错误继续等待，服务重启可依据数据库记录查询并完成结算，明确失败解冻，恢复过程不重新下单。
- 阻止过期任务保存把 completed 写回 processing/reconciling/failed；任务数据库写入失败不再被误报为幂等成功。
- 图片/音频先校验结果再结算；音频拒绝空响应、JSON 和 HTML 错误页，WaveSpeed 音频下载限制大小并使用安全下载客户端。
- 视频轮询严格使用原渠道；处理中或失败响应里的预览/查询 URL 不再被误认为完成视频。
- 动态 Token 档位在真实计费未实现前禁止生成；模型广场空数组/停用档位过滤补齐，WaveSpeed 协议按供应商标识明确选择。
- 3D 页面只使用 Seed3D 档位、平台渠道和实时公开售价，先确认费用，不再沿用画布视频模型。
- 财务页明确区分已记录估算成本与已核实成本；上游额度不足不再提示为本站钱包余额不足，公开错误不返回供应商原始信息。
- PostgreSQL DSN 不再被当作保存 JWT 密钥的本地文件路径；新增字段只通过兼容迁移添加。
- 本地 HTTP 集成测试覆盖注册/鉴权、A/B 用户隔离、画布保存、生成去重、503 后恢复、资金结算、失败解冻及无原工作进程的图片/音频恢复。

以上不等于线上或真实模型验收。当前仍需支付、持久化数据库、真实存储和逐模型效果验收；原有待测试项保留如下，历史价格/数量以重新核验结果为准。

- 钱包会独立读取公开支付状态；即使登录令牌失效或钱包记录请求失败，也会明确显示数据库、商户或配置读取原因，不再只留下无法点击且没有解释的按钮。
- 默认模型目录初始化改为无告警查询；临时库首次建表时不再用大量正常的 `record not found` 淹没 Render 日志，模型和线路创建行为保持不变。
- staging 的 LEC Render Secret 变量名已恢复为 `MODEL_PROVIDER_LEC_API_KEY`；模型广场重新出现 Seedance 2.0/2.5，仍需登录后台复查上线检查并逐条小额验证。

- 钱包新增支付宝原路退款：只允许对已到账支付宝订单申请且仅可退当前未消费余额；申请时立即锁定站内余额，拒绝或支付宝明确失败时自动恢复。管理员确认会真实调用 `alipay.trade.refund`，结果不明确时保持处理中并使用原退款请求号查询，禁止自动释放或重复退款。需在 staging 用小额真实订单分别验证成功、拒绝、重复审核和网络超时后查询。
- 财务中心新增支付宝退款金额、充值净额与退款处理中余额；成功退款按确认时间计入所选期间，待审核/处理中金额仍计入平台未消费资金责任。

- 视频任务已经取得真实上游任务 ID 后，轮询阶段遇到 404/429/5xx、线路暂时不可读或网络错误时只进入“等待上游确认”，不会把一次查询失败误判成生成失败或提前退款；只有上游任务载荷明确返回失败状态才解冻。
- 视频任务超过 20 分钟后改为每分钟继续追踪，不再因为进入对账状态而停止轮询；最近 48 小时内仍会恢复成功结果。需在 staging 用可控的 502、线路暂时不可用和迟到成功场景验证。
- 不再限制同一用户只能有一条 LEC 视频任务；不同客户端任务 ID 可以并行提交，同一个客户端任务 ID 仍由数据库幂等锁保证只提交、冻结和结算一次。
- Provider 已接单后保存本地任务映射会自动重试三次，降低“上游已有任务、本地没有上游任务 ID”的概率；若三次都失败仍保持冻结并进入人工对账，禁止自动重复提交。
- 模型广场可用性汇总改为一次读取 Provider 列表，不再按每条线路重复查询 Provider；需在 staging 对比 `/api/model-market` 响应时间。
- Seedance 2.0“人物特惠 720P / 900”在 staging 已确认路由为启用的 `lec-seed-2-0-900`，但尚未在本次修复后再次发起付费任务。

- 固定 480P/720P/1080P 的视频档位会锁定并明确展示真实清晰度；选择档位时同步节点清晰度，画布播放后显示媒体文件实际像素，避免 480P 线路伪装成 720P。

- GPT Image 2 的 16:9 等 WaveSpeed 支持画幅会从前端像素尺寸还原为 `aspect_ratio`，不再因只识别 1:1、3:2、2:3 而静默丢失；需在 staging 用 16:9 生成验证实际输出尺寸。
- 画布异步任务状态刷新由 5 秒缩短为 2 秒，并区分“正在提交任务”和“上游生成中”；这只减少完成后的回显延迟，不承诺缩短上游模型本身的排队/生成时间。

- `/admin/finance` 增加今日、昨日、7 天、30 天和累计筛选；首屏分开显示生成收入、实际上游成本、毛差、费用、净收益、失败解冻与 Provider Reserve，并增加模型利润排行和四家 Provider 成本统计。筛选期费用只扣除同一期间费用。
- 普通用户新增 `/generation-history` 和画布“最近任务”面板；接口强制按登录用户 ID 查询，只返回公开模型、状态、售价、退款、耗时、结果和友好错误，不返回 Provider、上游模型、成本、毛差、Key、原始响应或内部错误。
- 用户生成记录每 5 秒刷新异步任务状态，页面刷新或重新登录后仍从数据库恢复；成功任务可预览/下载并复制参数，失败任务明确显示已退回金额。
- 财务准备金查询已按真实表结构拆分：视频按 `billing_status`，图片/音频按任务状态；不再向不存在 `billing_status` 的图片/音频表发起查询。
- 本地新增并通过用户任务隔离测试和期间费用口径测试；仍需 staging 验证用户 A/B 隔离、普通接口字段白名单、财务真实汇总和管理员完整日志。

- 钱包沿用 `users.credits`、`users.frozen_credits`、`credit_logs` 和 `recharge_orders`，新增支付回执、Provider 资金流水与运营费用表；生产数据库只做兼容迁移，不清库。
- 视频成功结算在单事务内完成冻结余额结算、上游成本流水、实际成本、成本来源和毛差；失败完整解冻，并独立记录上游退款状态。
- 支付宝异步回调新增 `seller_id` 校验，第三方 `trade_no` 通过独立支付回执主键保持全局唯一，重复 notify 不重复入账。
- 管理员余额调整改为只有 `super_admin` 可做的增量调账，原因必填，余额与 `admin_adjustment` 流水同事务提交；配置页不能再静默覆盖上游余额。
- `/admin/finance` 展示未消费预付余额、已消费收入、实际上游成本、成本准备金、毛差、支付手续费、补偿、运营费用和估算净收益，并可登记运营费用。
- 管理后台前端统一将 `admin` 与 `super_admin` 识别为管理员；普通用户仍不能进入 `/admin/*`，后端 `AdminAuth` 同样复用统一角色判断。
- `/admin/providers` 可登记已手工完成的上游充值并查看独立 Provider 流水；系统不会自动登录中转站、保存支付密码或自动充值。
- 本地 A～G 资金测试、全仓 Go 测试、TypeScript 检查和 Next.js 生产构建已通过；仍需 staging 验证 PostgreSQL/Neon 迁移、真实支付宝回调和历史生产数据汇总。

- 画布重新打开后会按 `source_id` 找回 WaveSpeed/海螺、LEC 等平台异步视频任务；后台已完成的视频应自动回填节点，已退款的超时任务在上游迟到成功时只恢复结果、不重复扣款。
- 视频任务生成 20 分钟后进入低频对账但不停止轮询；最近 48 小时内因旧版超时规则退款的任务会自动复查一次，迟到成功只恢复结果、不再次扣用户余额。

- 提示词库在云端没有同步数据或请求失败时，展示 12 条内置创作模板，并支持搜索、分类、标签、复制和加入我的素材。
- 主导航新增“素材库”，未登录也可访问；云端没有素材时展示 12 条内置创作工具素材。
- 配置弹窗新增“查看余额”和“去充值”入口，均跳转到 Z-API 钱包页；画布不保存支付信息。
- 站内“我的钱包”展示可用余额、冻结余额、充值订单和本人最近 100 条钱包流水；支付返回后自动刷新订单与余额，网关异步通知验签成功后原子到账。
- 钱包在线充值改为仅展示支付宝；后端支持支付宝开放平台网页支付 RSA2 下单与异步通知验签，并校验 App ID、订单金额和交易状态。正式可用仍需完成支付宝网站支付签约、密钥配置、应用审核与小额实付验证。
- 支付宝密钥配置完成但网站支付产品尚未签约或应用未审核时，钱包保持关闭并展示真实原因；只有验证完成并设置 `ALIPAY_PAYMENT_ENABLED=true` 后才开放付款按钮。
- 管理后台继续通过用户管理、AI 日志、生成日志和算力点日志查看登录用户及其生图/视频调用，普通用户无权访问。
- 模型广场由后端目录提供 27 张模型卡、94 个档位，并按热门、大语言模型、图片、视频、人物、音乐、语音、3D 和工具分类；公开接口只返回当前确有可用线路的模型，未配置线路的档位不会假装可用。
- 新增 `Providers + Models + Routes` 管理页，可保存供应商 Base URL 和自有模型到实际上游模型 ID 的线路映射；上游 API Key 只来自服务器 Secret，后台仅显示是否已配置。
- 模型广场点击可用卡片后，应把自有模型 ID 写入画布配置并打开画布；用户界面不显示供应商名称和实际上游模型 ID。
- JSON 与 multipart 请求使用档位 ID 时，后端应按启用线路解析供应商并替换为实际上游模型 ID；同步图片、音频和异步视频创建会按线路优先级尝试主备线路，只对收到明确 408/425/429/5xx 响应的创建请求切换，连接中断等结果不确定的错误不自动重试。
- 模型卡片的分辨率、时长、比例、模式或价格为 `null` 时，前端 API 层应统一转换为空数组，音频和工具模型不再导致模型广场整页崩溃。
- 用户钱包、顶部余额、画布生成按钮和模型广场统一使用人民币显示；内部继续以人民币分整数记账，充值金额与到账余额一比一，不再向用户展示算力点。
- 模型广场新增 GPT Image 2 默认/2K/4K/Pro 4K、Midjourney 标准/Turbo 和 Wan Ultra Fast 10 秒独立卡片，并按已确定的薄利售价展示；这里只录用户售价，未核实成本不向用户展示。
- 模型广场目录升级为热门、大语言模型、图片、视频、人物/数字人、音乐、语音、3D、AI 工具；语言模型同时展示输入和输出的每百万 Token 售价，其余模型按张、次或秒展示人民币售价。
- 模型广场以“最终接入定价”表为唯一默认目录，当前为 27 张模型卡和 94 个档位；同一模型只显示一张卡，用户在卡片内选择具体档位。
- 后台只允许 302.AI、WaveSpeed、LEC、seedance.nz 四家供应商；档位只有在 Base URL、服务器 Secret、真实上游 ID 和固定售价都满足时才允许生成。
- 公开模型广场接口只返回前台名称、档位、售价和可用状态，不返回供应商代码、上游 Model ID、成本价或上游 Key。
- 普通用户保存模型配置时会强制使用平台云端模式并删除本地 API Key/Base URL/本地渠道；普通用户提交本地渠道请求会被后端拒绝，本地参数转译接口仅管理员可访问。
- 管理后台“模型与路由”新增档位售价/上下架编辑，并将线路配置改为精确关联模型档位。
- 异步视频任务应以公开档位 ID 记账并返回给用户，同时只在服务器任务记录中保存真实上游模型 ID；轮询继续使用上游模型，普通用户响应不返回渠道名称、渠道 ID、上游模型或原始请求体。
- 供应商即使被管理员设为启用，Base URL 或 API Key 为空时也不得使模型档位显示为可用；动态成本档位在真实结算未实现前不得生成。
- 目录版本升级后，四家上游遗留在 `model_providers.api_key` 的明文应被清空；分别用 `MODEL_PROVIDER_302_API_KEY`、`MODEL_PROVIDER_WAVESPEED_API_KEY`、`MODEL_PROVIDER_LEC_API_KEY`、`MODEL_PROVIDER_SEEDANCE_NZ_API_KEY` 提供服务器密钥。
- 用户钱包新增可用余额与冻结余额；生成前原子冻结售价，成功后结算，失败后解冻，重复任务回调通过账务流水 ID 保持幂等，不得重复扣款或退款。
- 同步图片、音频和工作流 Agent 请求，以及异步视频创建与轮询，均应使用同一套冻结、结算、解冻流程；异步视频在最终状态出现前保持冻结。
- 管理员新增“生成任务”页面，可查看最近图片、视频、音频任务的用户、公开模型档位、任务状态、资金状态、售价、结果和错误；普通用户无权访问该接口。
- 钱包流水后台改为只读，展示可用余额与冻结余额两组变动；管理员仍只能通过用户余额调整或充值审核产生受控流水。
- 视频任务完成记录保留 30 天以供后台追踪；Base64 或本地对象地址不作为管理员可点击的结果链接返回，避免把大块媒体数据塞入管理接口。
- 同一套站点按 `admin` / `user` 角色展示不同能力：管理员可使用普通画布并额外进入管理后台，普通用户只使用平台云端渠道且看不到上游 Key。
- 管理后台首页改为“财务中心”，展示今日与累计充值到账、生成结算收入、失败解冻、用户可用余额和任务冻结余额；未取得上游真实成本前明确不展示利润或伪造供应商余额。
- [ ] 修改 Render `ADMIN_PASSWORD` 后重新部署，确认数据库中的 `admin` 管理员密码同步更新，并可进入 `/admin/finance`。
- 管理后台新增独立“中转站管理”页面，集中展示四家上游的 Base URL、服务器 Key 配置状态、启用线路数、手工核实余额和黄/红/极低余额预警；没有余额记录时显示“需要登录上游查看”。
- “模型与路由”新增完整模型管理：管理员可编辑模型名称、分类、描述、热门推荐、运行状态、排序和模型广场上下架；已隐藏模型仍保留在管理员列表中并可重新上架，档位售价改为直接填写人民币。
- 画布图片和音频使用模型广场档位时不再错误要求旧版渠道 ID；生成应直接使用后台启用的路由，所有线路失败后只解冻一次。
- 图片和音频任务在尚未提交上游时超时会失败解冻；已经提交上游的任务改为等待对账。视频取得上游任务 ID 后只在上游明确失败时解冻，查询超时或临时 5xx 不作为最终失败。
- 管理后台“生成任务”支持按关键字、类型、任务状态、资金状态和创建日期筛选；关键字可匹配任务、用户、模型、来源和错误信息。
- “模型与路由”新增上线检查，可汇总可用供应商、已上架档位、可生成档位、启用线路和当前阻塞项；缺少固定售价、上游 ID、Base URL、服务器 Secret 或供应商未启用时，后台禁止误启用线路。
- 画布模型选择器按图片、视频、文本和音频展示模型广场中真正可用的档位；旧节点保存的失效模型会回退到该类别当前首个可用档位，生成按钮读取模型广场人民币售价，不再显示旧模型或 ¥0.00。
- 管理后台“模型与路由”任一资料接口失败时，其余中转站、模型、档位和线路仍应独立显示，并明确提示具体失败项，不再因一个请求失败整页显示 0。
- 首次目录升级时，中转站、模型、线路和档位应顺序读取，避免四个 `ensureDefaultModelCatalog` 事务同时争抢目录版本锁而长期显示加载中。
- 中转站连接测试返回模型 ID 时，后台应弹窗列出实际上游模型清单；清单不显示 API Key，可用于逐个核对尚未接入的真实线路。
- 模型广场的 `market:*` 仅作为前端分组标记，不再写入真实渠道请求头；Seedance 2.0 人物特惠 720P（900）选择非 10/15 秒时应在请求前提示支持时长。
- 旧画布的生成配置即使没有新版 `@[node:*]` 引用标记，也应继续读取所有实际连入的文本、图片、视频和音频节点，图生视频不得静默丢失参考图片。
# 2026-08-30 生成任务后台

- 部署后打开 `/admin/generation-tasks`，确认历史调用日志标记为“历史调用日志”，显示渠道和上游任务号。
- 确认成功图片/视频可在表格内播放或预览，失败任务显示上游错误。
- 新建一次视频任务，确认本地 `video_tasks` 与历史调用日志不会重复展示。
# 2026-08-30 视频任务状态闭环

- 构造超过 2 小时且上游仍 processing 的任务，确认转为 reconciling、余额仍 frozen。
- 确认 reconciling 任务上游 completed 后保存视频并 settled；明确 failed 后才 released。
- 管理员生成任务点击“放大预览”，确认图片和视频弹窗正常显示。

# 2026-08-30 上游模型目录同步

- 302.AI、LEC 和 WaveSpeed 启动后自动读取权威模型目录，停用目录中已经不存在的精确上游线路；历史任务和模型记录不删除。
- 管理后台允许 super_admin 手动触发三家目录同步；seedance.nz 未确认权威目录接口前保持不支持自动同步。
# Render 持久化与公开入口

- Render Blueprint 改为要求显式提供外部 PostgreSQL `DATABASE_DSN`，不再引用项目中并不存在的 Render 数据库资源。
- `PUBLIC_BASE_URL` 已修正为当前 staging 域名，支付回调和媒体引用不再指向旧地址。
- `/register` 现在会进入登录页的注册模式，直接访问注册链接不再显示 404。
- 页面 GitHub 入口已改为当前维护仓库 `Txium/infinite-canvas`。
- 部署前仍需在 Render 创建或同步 Blueprint 数据库；数据库连接成功后验证注册、登录、跨设备画布同步和钱包流水。

# WaveSpeed 数字人、语音与 3D

- 模型广场新增 InfiniteTalk 480P/720P 数字人口播、MiniMax Speech 2.6 HD 文字转语音和 Seed3D 2.0 图片转 GLB，线路只使用已核对的 WaveSpeed 官方端点。
- WaveSpeed `/models` 不再误停用人物、语音、音乐、3D 和工具类异步端点；这些类别必须按专用 API 页面核对。
- InfiniteTalk 会校验人物图和口播音频并按官方 JSON 字段提交；Speech 2.6 HD 会轮询异步任务后下载音频；Seed3D 生成完成后提供 GLB 下载页。
- 本地 TypeScript 检查和 Next.js 生产构建已通过；仍需在 staging 分别做一次小额数字人、语音和 3D 实付生成，核对上游结果、钱包冻结/结算和失败解冻。

# 2026-09-02 模型广场冷启动与画布空功能修复

- 模型广场首次请求失败时自动分段重试三次，仍失败则显示真实连接提示与“重新加载”按钮，不再把接口故障误显示成“暂无模型”。
- 模型卡加载期间显示骨架屏；只显示至少有一个可用模型的分类，避免用户进入没有可用线路的空分类。
- 图片节点的“AI 超分”不再打开“暂未实现”空弹窗；现在会先说明按当前图片模型价格扣费，确认后以原图为参考生成高保真修复子节点，失败时保留错误状态且不覆盖原图。
- 本地 TypeScript 检查、Next.js 生产构建、全仓 Go 测试均已通过。
- 部署后需用 Render 冷启动场景验证自动重试与手动重载；AI 超分需由管理员使用小额图片任务核对实际保真度、钱包冻结/结算和失败解冻，本次未产生付费调用。

# 2026-09-02 WaveSpeed 图片模型价格与参考图核对

- 按 WaveSpeed 当前官方文档修正 Krea 2 售价：Turbo 统一按 2K 最高成本计价，Medium 按 $0.03/张计价，Large 按带参考图 $0.065/张的最高成本计价，均保留小额人民币毛差。

# 2026-09-02 四家中转站价格复核

- WaveSpeed 当前已启用的图片、视频、数字人、语音、3D 与大语言模型未发现需要上调的官方价格；302.AI Midjourney Fast/Turbo 仍为 0.05/0.10 PTC 每次；seedance.nz 继续按实际 Token 与超分动态结算，不改成伪造的固定价。
- LEC 同步后保留 16 条实际可用线路。Seedance 2.0 900 专线成本由 ¥1.50 降为 ¥1.30/次，MD 线路由 ¥1.50 降为 ¥1.20/次，前台售价同步下调。
- Seedance 2.5 900 专线当前成本为 ¥3.00/次，原 ¥2.29 售价低于上游成本，已调为 ¥3.29/次；AC 900 成本由 ¥4.00 降为 ¥3.00/次，售价调为 ¥3.39/次。
- 修复 Seedance 2.5 AC 480p 与满血档把上游“按秒”误写成“按次”的高风险计费错误；分别按 ¥0.51/秒和 ¥0.92/秒出售，后者按 720p ¥0.82/秒最高成本保护。
- Krea 2 Turbo 只向上游发送第一张 `image`；Medium 参考图在提交付费任务前明确拒绝；Large 将最多 10 张图片转换为官方 `reference[{image_url,strength}]` 格式。
- GPT Image 2 九档质量/分辨率与当前官方 $0.01–$0.72 价格表一致，本轮不调整。
- 本地测试通过后仍需分别做一次 Krea Turbo 2K、Medium 文生图和 Large 参考图小额实付验证，本轮未产生付费调用。

# 2026-09-02 WaveSpeed 图片端点纠正

- Nano Banana 2、Nano Banana Pro 和 FLUX 2 Klein 并未下架；此前同步停用是因为画布保存的上游 ID 缺少当前官方端点后缀或仍使用旧别名。
- Nano Banana 2 改为 `google/nano-banana-2/text-to-image` 并固定 1K，售价保持 ¥0.52/张；Nano Banana Pro 改为 `google/nano-banana-pro/text-to-image` 并固定 2K，售价保持 ¥1.05/张。
- FLUX 2 Klein 改为 `wavespeed-ai/flux-2-klein-4b/text-to-image`，售价保持 ¥0.06/张；目录升级后重新启用三条线路。
- 后端不再把前端 `quality` 原样传给 Nano Banana，防止用户请求 4K 时仍按固定低档售价扣费；本轮未发起任何付费图片生成。

# 2026-09-02 充值手续费后的模型售价保护

- 对本轮核价的 10 个固定成本档位按 `售价 × 94% >= 上游成本 × 120%` 设置价格下限，覆盖 6% 充值手续费后仍保留至少约 20% 的成本毛利。
- Seedance 2.0 900/MD 调为 ¥1.69/次、¥1.59/次；Seedance 2.5 默认渠道1、900 专线与 AC 900 均调为 ¥3.89/次，AC 480P 与满血 AC 调为 ¥0.59/秒、¥1.09/秒。
- Nano Banana、Nano Banana Pro、FLUX 2 Klein 调为 ¥0.65、¥1.29、¥0.09/张；本轮仅修改目录售价，不发起付费生成。
