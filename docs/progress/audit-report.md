# 画布前后端排查与修复报告

## 结论与范围

### 最新执行进度（2026-09-10）

- 首批修复 `0f61d40fc7e0e81b93f9722e7acfc55640f1c02b` 已推送并在 staging 成功部署，Render 部署 `dep-dah6n3h42hec73f1jcfg` 显示 Live，`/api/health` 返回 ok，原有两张画布和测试 Logo 保留。
- 推送后未触发自动部署，改用指定提交部署；Render 此方式会关闭自动部署。后续补丁继续明确指定 staging 提交，不触碰 main。
- 新版 622px 实测发现尺寸监听首次执行时仅有加载壳，真实画布挂载后未重绑，状态仍为 1200px。现已改为随 projectLoaded 绑定/清理，并删除会覆盖已保存视图的测量时自动居中；新增生命周期回归，前端 6/6、TypeScript 和 Next.js 生产构建通过，补丁待再次部署视觉验收。
- 新版供应商页面已显示原值及核实时间：WaveSpeed 5.92 USD、seedance.nz 10.38277 CUSTOM；302 仍为余额权限 403，LEC 不伪造余额。

本轮修复了代码中可复现的资金安全、任务恢复、模型路由、余额持久化、自动转存与窄屏布局问题，并完成本地自动化验证。**尚不能认定全部功能已经上线可用，也不建议现在开放正式收款。** staging 已确认管理员登录、数据库/存储就绪标记和测试图片上传后刷新读取；新增数据库列、真实支付和各模型实际效果仍需部署后验收。动态 Token 计费等开发项仍未完成，已安全禁用。

- 依据用户提供的 `pending-test.md` 与 `todo.md`，原文件未修改。
- 修改项目：`E:\Infinite-Canvas-remote`，Go/Gin/GORM 后端与 Next.js 前端。
- 分支：`staging-wallet`。先从本地 `3a094b1` 快进同步到远端 `611462c`，包含附件提及的后续修复，再开展本轮修改。
- 旧 Python 项目 `E:\Infinite-Canvas` 及其用户修改保持不动。本次仅准备发布到现有 staging；正式站仍使用 `main`，不修改正式站配置。
- 全部生成和金融测试使用临时 SQLite、测试账号和模拟上游；未使用真实 Key 发起付费生成、充值或退款。
- 2026-09-10 在 staging 创建了“修复验收 2026-09-10”并上传项目公开 Logo；只操作该测试画布，保留原有两张画布。用户随后自行登录 Render，已确认有 staging 服务部署权限；不复用画布管理员密码登录其他服务。

### 部署前核对

- 目标为 `infinite-canvas-staging`，关联 `staging-wallet`；当前运行提交为 `611462c52123297152a129471dc1e32fd337fdae`。
- 正式站 `infinite-canvas` 关联 `main`，本次不推送该分支，不触发正式站部署。
- Render 线上实际设置为 `On Commit`，与仓库 Blueprint 中 `off` 不一致；沿用现有设置，推送后只跟进已有部署，不重复点击发布。
- 数据模型仅新增图片/音频上游任务号和五个供应商余额快照字段，不删除/重命名现有字段、不清库。完整云数据库备份尚未核验；部署后检查数据库读取及原有画布保留情况。

“本地通过”仅指下述测试覆盖的行为，不表示所有供应商、所有规格、浏览器交互或 PostgreSQL 并发已验收。

## 本轮修复的具体问题

| 编号 | 问题与影响 | 处理与验证 |
| --- | --- | --- |
| F01 | 旧 `/auth/me` 响应覆盖新账号或较新的余额；旧 401 误退出当前账号 | 以请求序号和 token 双重校验；5 项前端状态测试覆盖账号切换、乱序和网络故障 |
| F02 | 钱包/财务请求乱序、Promise 错误缺少处理，退款/费用按钮可重复点击 | 清理旧状态、忽略过期响应、增加错误提示和提交锁；类型检查及构建通过，实际点击待测 |
| F03 | `null` 模型卡数据、停用档位影响前端模型列表 | API 层规范化空值并过滤不可用档位；前端单元测试通过 |
| F04 | 3D 页面可能继承当前视频模型并错误下单 | 固定使用 Seed3D 类档位和平台渠道，读取公开售价后确认；类型检查及构建通过，GLB 实测待验收 |
| F05 | 前端生产构建允许忽略 TypeScript 错误 | 关闭 `ignoreBuildErrors`，最终生产构建包含真实类型检查 |
| F06 | 财务把 estimated 成本称作“实际上游成本 / 已实现毛差” | 后端存在估算时不标记全部核实；前端注明含估算及待核账；新增财务回归通过 |
| F07 | 622px 侧面窗口中侧栏挤占空间，顶部按钮覆盖标题，工具栏固定左偏移导致无法正常使用 | 窄屏侧栏临时浮层、不改用户桌面偏好；标题/按钮控制收缩，工具栏按画布容器宽度分层；生产构建通过，新版视觉验收待部署 |
| B01 | 视频成功结算用数据库旧状态覆盖刚轮询到的成功 URL | 同事务保留新状态、URL、上游映射与响应，冻结金额结算及成本流水保持原子；回归通过 |
| B02 | 同一任务结算/解冻都成功，可能动用另一任务冻结额 | 锁定用户并检查相反终态流水；重复同操作幂等、相反操作拒绝；回归通过 |
| B03 | 旧任务已退款但状态未及时写入，迟到成功再次扣款 | 检查已退款流水，只恢复结果、不再次收费；用户历史保留 completed 和退款金额；回归通过 |
| B04 | 账务 ID 冲突和跨用户重放被误当作重复成功 | 校验用户、任务、金额及冻结额；任务插入错误和数据库读取错误如实返回；回归通过 |
| B05 | 外部请求头可伪装服务器内部任务计费上下文 | 改为服务器私有 context；不信任客户端 `X-Billing-*`；回归通过 |
| B06 | multipart 上游请求未替换公开档位为真实模型 | 保留 boundary、参考图与其他字段，重写 model；回归通过 |
| B07 | 非整数数量、畸大时长或整数乘法溢出导致错误冻结额 | JSON/multipart 完整数值校验、秒数向上取整及乘法溢出检查；保留已有 -1 自动时长约定；回归通过 |
| B08 | 动态档位填公式后被当成可售，但没有实际 Token 结算 | 可用性、路由保存和实际请求入口一致禁止动态档位；真正 Token 计费仍列为待办 |
| B09 | 已接单图片/音频遇查询错误提前退款，重启后没有任务号可恢复 | 新增仅服务端可见的上游任务号；WaveSpeed 接单先持久化，查询不确定则保持冻结；无原工作进程恢复测试通过 |
| B10 | 旧工作进程覆盖已完成任务或擦掉新保存的上游任务号 | 数据库更新保护终态和映射；重启扫描包含 reconciling，轮询更新时间避免队首长期独占；回归通过 |
| B11 | 图片/音频响应尚未校验就收费，错误页可能当作音频 | 内部任务推迟到结果校验后结算；拒绝空/JSON/HTML 音频、限制 WaveSpeed 下载大小并使用安全客户端；回归通过 |
| B12 | 原视频线路不可用时可能拿其他供应商查询已接单任务 | 原线路不可用保持对账，不跨供应商查询或重新下单；既有及新增回归通过 |
| B13 | processing/failed 响应附带 URL 被当作成功视频 | 非 completed 状态不认作结果链接；WaveSpeed/nested URL 及负向解析测试通过 |
| B14 | 已完成充值订单携带另一交易号仍被接受 | 锁定订单并校验重复回调交易号一致；现有支付唯一回执约束保留；回归通过 |
| B15 | 退款查询迟到错误可能改写终态；释放接口目标状态缺少约束 | 仅 processing 可更新查询信息，拒绝不合法释放状态；全仓资金测试通过，真实支付宝待测 |
| B16 | 用户余额和 Provider 手工充值的并发读改写风险 | 使用事务行锁保护读取与更新；SQLite 回归通过，PostgreSQL 并发验收仍保留 |
| B17 | 普通用户查看后台返回 401、上游原始错误可能泄漏细节或误报余额 | 无登录 401 / 权限不足 403；公开错误白名单，上游 wallet quota 区分本站钱包；接口隔离及错误测试通过 |
| B18 | PostgreSQL 连接串被作为 JWT 文件路径处理 | 仅 SQLite DSN 可用于推导本地密钥目录；PostgreSQL 回归通过 |
| B19 | 上游实时余额刷新页面即丢失，缺失字段可能误显示零余额 | 独立保存原值/币种/核实时间；失败保留，拒绝乱序及旧地址响应，配置保存不能覆盖；解析、数据库与 HTTP 回归通过 |
| B20 | 后台转存丢失管理员角色，音频仅保留 Base64，迟到完成响应可能覆盖已转存链接 | 查询真实活动用户，不提权；原字节音频直接转存并保留失败回退，数据库原子保护已存储结果；模拟 S3 字节摘要、所有者权限及迟到响应回归通过 |

## 对照附件的逐项检查矩阵

来源简写：`P` 为原附件 pending-test.md 行号，`T` 为原附件 todo.md 行号。重复条目合并在同一行，保留原文定位。“代码核对”不是浏览器/云端验收。

| 附件定位 | 前端/后端检查项 | 当前结论及剩余验收 |
| --- | --- | --- |
| P8、48、57；T17 | 钱包状态、人民币、支付返回刷新 | F01/F02 已修；本地状态测试通过；支付宝回跳和真实到账待测 |
| P9、19、84 | 目录初始化、查询次数与冷启动 | 保留同步后的顺序加载/目录实现；未测 Render 冷启动耗时及多实例锁竞争 |
| P10、20、76、85；T10、15、21 | Provider Secret、余额查询、线路和后台连接测试 | 线上四家显示 Key 已配置；WaveSpeed 查询 5.92 USD、seedance.nz 查询 10.38 CUSTOM、302 余额权限 403、LEC 列模型成功。原币快照持久化代码 B19 完成，尚未部署；不读取或展示真实 Key |
| P12、13、35、49、50；T17、19、22 | 支付宝付款/退款/验签及幂等 | B14/B15 及现有金融单测通过；未真实调用支付宝，不改变收款开关，商户审批和实付待测 |
| P15、16、18、42、43、79、95、96；T12、33、34 | 异步任务不确定状态、低频轮询和迟到成功 | B01/B03/B09/B10/B12/B13 已修；本地恢复通过；真实长任务和 48 小时历史回收待测 |
| P17、68、69 | 并行任务、冻结/结算/解冻互斥 | B02/B04/B05 已修；本地去重、相反终态、跨用户重放通过；PostgreSQL 高并发待测 |
| P22、24、86、87 | 锁定清晰度、画幅还原、时长限制和旧节点参考素材 | 核对现有适配与前端路径，保留原修复；输出像素/时长及素材一致性必须用真实生成确认 |
| P25、29 | 画布轮询、刷新后恢复、预览下载 | 本地持久化查询和恢复通过；浏览器节点回填、播放/下载及多设备仍待测 |
| P27、30、31、34、37、74；T16、20 | 财务期间汇总、成本准备金、实际成本及利润 | F06/B01 已修，现有期间费用回归通过；成本缺失/estimated 仍不是核实利润，真实账单与费用期间待核账 |
| P28、62、65、72 | 公开历史用户隔离和字段白名单 | 注册 A/B、画布隔离、任务隔离、公开历史秘密字段排除及服务响应测试通过 |
| P33、40、105、106、109；T19 | PostgreSQL/Neon、兼容迁移及 Render 地址 | Blueprint 保留显式外部 DSN 和 staging URL；B18 已修；本地 SQLite 自动迁移通过，云端迁移未执行 |
| P36、39、71 | 管理员增量调账、Provider 流水、只读钱包记录 | 代码核对及金融回归通过，B16 增加并发锁；未改真实余额、未登录上游自动充值 |
| P38、51、70、73 | 管理员角色和后台权限 | B17 修正 401/403，HTTP 普通用户禁止访问后台测试通过；staging 管理员登录、财务页和中转站页已实点 |
| P45、46 | 提示词、素材库内置内容和搜索复制 | 核对现有页面及 API；构建通过；未完成断网/搜索/收藏等浏览器人工验收 |
| P47 | 配置弹窗 Z-API 余额入口 | 核对仍在旧本地直连配置区域，不能当作平台钱包；不扩展普通用户的上游配置权限 |
| P52、58、59、60、113 | 模型分类、单卡多档、数字人/语音/3D | 目录与页面存在；F03/F04 处理可用性/专用模型；不沿用旧文档数量作为当前上线数量 |
| P53、61、63、67、81 | 四家供应商、服务器密钥、普通用户云端限定 | 代码边界保留；mock 接口与公开响应验证无测试 Key 泄漏；真实 Secret/历史库明文迁移待云端确认 |
| P54、64、77、82 | 模型卡跳画布、档位编辑、旧模型回退 | F03/F04 已修相关可用性；类型检查通过；后台编辑/画布选择器交互待测 |
| P55、78 | JSON/multipart 精确路由、主备切换、不确定请求不重试 | B05/B06/B09/B12 已修；mock 503 查询恢复、只创建一次通过；各供应商创建失败切换仍待实际合同验证 |
| P56、83、120、121 | null 数据、部分接口失败、自动重试和骨架屏 | F03 单测通过，保留现有分段重试/独立加载；Render 冷启动与浏览器手动重载待测 |
| P66；T14 | 动态 Token 实际计费 | 仍未实现；B08 已安全禁用，不能把“公式已填写”当作完成。Kimi/GLM 实时成本加价规则未验收 |
| P75 | Render 管理员密码更新和重新部署 | 本轮未更改密码、未重部署；待云端管理员操作验收 |
| P80、90、91、92、97；T27–30、35、36 | 管理员任务筛选、历史合并、去重和放大预览 | 现有代码/测试保留；本地类型检查通过；不把附件中的历史“8 条记录/4 个视频”当作本轮实测 |
| P101、102、114 | 权威模型目录同步及特殊端点豁免 | 保留现有同步实现；本轮没有对真实四家账号发同步请求，最新目录和权限待确认 |
| P107、108 | 注册路径、GitHub 入口 | 生产构建包含 /register；HTTP 注册/鉴权通过；浏览器跳转未实点 |
| P115、116；T12 | InfiniteTalk、Speech、Seed3D 请求和输出 | F04/B09/B11 相关修复、音频恢复 mock 通过；真实数字人/语音/GLB 质量、扣费待实测 |
| P122、124；T24 | AI 超分 | 保留参考图修复流程与付费确认；未生成付费结果，不承诺逐像素一致 |
| P123 | Go、TypeScript、Next 构建 | 本轮重新执行，全部通过；不是引用旧记录 |
| P128、132–138、142–145、149–151；T11、13 | Krea/Nano/GPT Image/LEC 等端点、规格、价格 | 保留最新分支已核价目录；B06/B07 防模型错路由及计费数值错误；本轮未重新确认所有实时价格或实付，不把历史标价称为当前官方报价 |
| T18、23 | 生成结果永久转存 | 按 Supabase S3 兼容规范保留 endpoint 路径签名修复；staging 上传公开 Logo 后站内读取/刷新成功；B20 自动转存权限及音频已修。付费生成转存、跨设备和失败重试仍待验收 |

## 实际执行的验证

| 验证 | 结果 | 边界 |
| --- | --- | --- |
| `go test -json ./... -count=1` | 150 个测试及子测试 pass；6 个有测试包 pass；0 fail | 使用临时 SQLite 与模拟上游；不是 150 个真实模型测试 |
| `go vet ./...` | 通过 | 静态检查 |
| `node --test scripts/audit.test.cjs` | 5/5 通过 | 实际 TypeScript 模块转译后测试状态逻辑；HTTP 被模拟，不是浏览器 E2E |
| `tsc --noEmit` | 通过 | 前端类型检查 |
| `next build` | 通过，29 个页面生成，构建内 TypeScript 检查开启 | 页面可构建，不代表按钮、播放、主题等视觉验收完成 |
| 本地隔离后端 `/api/health` | HTTP 200 | 临时端口 18080 的健康检查；测试服务已停止 |
| 浏览器验收 | 旧版 staging 部分通过 | 管理员登录、查询、测试画布重命名、上传和刷新后显示成功，已截图观察窄屏问题；新版本地前端启动曾被策略拦截，未绕过，新版视觉验证待部署 |
| `git diff --check` | 通过 | 不提交、不推送 |

新增主要证据：`router/audit_integration_test.go`、`repository/audit_regression_test.go`、`handler/audit_regression_test.go`、`web/scripts/audit.test.cjs`，以及配置、视频解析、公开响应和动态档位相关回归。

模拟业务链：注册 A/B → A 保存画布/B 无权读取 → A 提交图片 → 上游首次查询 503、随后完成 → 相同客户端 ID 不再创建任务 → 钱包结算且无冻结残留 → 公开历史不泄漏测试密钥或真实上游任务号。另一组仅预置已接单任务和冻结流水、不运行原工作进程，验证图片/音频恢复、明确失败解冻，恢复期间没有 POST 新单。

## 尚未完成的开发与上线验收

1. **开发项**：动态 Token 实际计费；可核验上游成本回传与调整流水；非 WaveSpeed 图片/音频及缺失上游 ID 的人工/自动对账。原币余额持久化已完成本地实现与测试，不再列为未开发，但尚未上线。
2. **数据库**：先备份 staging 并使用真实 PostgreSQL 兼容迁移；验证新增列、金融行锁、并发任务、重复回调和历史汇总。不能清库来让测试通过。
3. **存储**：线上已上传并读取测试 Logo；本地模拟存储已核对 SHA256/字节与所有者权限。仍需云端跨账号/跨设备以及生成结果转存失败重试队列；不得把已有临时链接或一次上传成功当成全链路完成。
4. **模型**：在明确预算与用户即时确认后，对需要开放的每个档位做小额实付，检查时长（需要 15 秒时必须是 15 秒）、像素、参考素材、可播放文件、实际收费和失败解冻。未测档位不要承诺效果。
5. **支付**：审批通过后用小额订单验证 RSA2/App ID/seller_id/金额、重复 notify、原路退款成功/失败/超时查询；此前保持收款关闭。
6. **部署及前端**：Render 登录阻塞已解除；实际自动部署开启。staging 部署后验证窄屏、余额刷新、钱包、素材库、任务恢复、下载和主题，验收成功才发布生产。

## 规范核对

本轮使用 Supabase 技能核对存储兼容性及变更记录，保留现有 Supabase endpoint 路径签名修复，没有创建或修改 Supabase 项目、密钥或权限。官方 [S3 兼容性说明](https://supabase.com/docs/guides/storage/s3/compatibility)说明兼容范围；它不能替代本项目真实上传和读回测试。财务/钱包弹窗修改参照 [Ant Design 官方资料](https://ant.design/llms-full.txt)并通过当前依赖版本类型检查。
